import { type Tone } from '../lib/ui';

export function SliderControl({ label, value, onChange, min, max, tone, left, right }: { label: string; value: number; onChange: (value: number) => void; min: number; max: number; tone: Tone; left: string; right: string }) { return <div><div className="flex items-center justify-between"><label className="text-sm font-medium">{label}</label><span className="font-display text-2xl">{value}%</span></div><input aria-label={label} type="range" min={min} max={max} value={value} onChange={e => onChange(Number(e.target.value))} className="mt-4 w-full accent-(--brand-lavender)" /><div className="mt-2 flex justify-between font-mono text-[9px] uppercase tracking-[0.12em] text-muted-foreground"><span>{left}</span><span>{right}</span></div></div> }
