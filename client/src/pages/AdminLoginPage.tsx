import { useState } from 'react';
import { Link, useLocation } from 'wouter';
import { Logo, Wordmark, PrimaryButton } from '../lib/ui';
import { useAuth } from '../lib/auth';
import { useLanguage } from '../lib/i18n';
import { LanguageSwitcher } from '../components/LanguageSwitcher';
import { ArrowLeft, ArrowRight, Eye, EyeOff, ShieldCheck } from 'lucide-react';

export function AdminLogin() {
  const [, navigate] = useLocation(); const { t } = useLanguage(); const { login } = useAuth(); const [username, setUsername] = useState(''); const [password, setPassword] = useState(''); const [showPassword, setShowPassword] = useState(false); const [error, setError] = useState(''); const [submitting, setSubmitting] = useState(false);
  const submit = async (event: React.FormEvent) => { event.preventDefault(); setSubmitting(true); setError(''); try { await login(username.trim(), password); navigate('/dashboard'); } catch { setError(t('admin.invalid')); } finally { setSubmitting(false); } };
  return <div className="min-h-screen bg-secondary text-foreground"><div className="mx-auto grid min-h-screen max-w-[1500px] md:grid-cols-[0.9fr_1.1fr]"><section className="relative hidden overflow-hidden bg-[#18231f] p-12 text-[#f5f1e8] md:flex md:flex-col md:justify-between"><div className="absolute inset-0 opacity-30 [background-image:radial-gradient(circle_at_20%_20%,#d9923b,transparent_30%),linear-gradient(rgba(245,241,232,.1)_1px,transparent_1px),linear-gradient(90deg,rgba(245,241,232,.1)_1px,transparent_1px)] [background-size:auto,58px_58px,58px_58px]" /><div className="relative z-10"><Logo inverse /><div className="mt-24 max-w-md"><Wordmark tone="saffron">Administrator access · prototype</Wordmark><h1 className="mt-5 font-display text-6xl leading-[0.92] tracking-[-0.055em]">Monitor where welfare delivery may need a closer look.</h1><p className="mt-6 max-w-sm text-sm leading-6 text-[#f5f1e8]/65">A focused workspace for district teams to connect signals, verify evidence and prioritize responsible action.</p></div></div><div className="relative z-10 flex items-center justify-between font-mono text-[10px] uppercase tracking-[0.14em] text-[#f5f1e8]/45"><span>{t('admin.demoData')}</span><LanguageSwitcher dark /></div></section><section className="flex flex-col justify-between p-6 md:p-12"><div className="flex items-center justify-between"><Link href="/citizen" className="inline-flex items-center gap-2 font-mono text-[10px] uppercase tracking-[0.14em] text-muted-foreground transition hover:text-foreground"><ArrowLeft size={14} /> {t('nav.backCitizen')}</Link><div className="md:hidden"><Logo /></div><div className="md:hidden"><LanguageSwitcher /></div></div><div className="mx-auto w-full max-w-md py-16"><Wordmark tone="indigo">{t('role.admin')}</Wordmark><h1 className="mt-5 font-display text-5xl tracking-[-0.05em]">{t('admin.loginTitle')}</h1><p className="mt-4 text-sm leading-6 text-muted-foreground">{t('admin.loginIntro')}</p><form onSubmit={submit} className="mt-10 space-y-5"><label className="block"><span className="text-sm font-medium">{t('admin.username')}</span><input required value={username} onChange={event => { setUsername(event.target.value); setError(''); }} className="mt-2 w-full rounded-xl border border-border bg-card px-4 py-3.5 text-sm outline-none transition focus:border-primary" placeholder="admin@sahayak.demo" /></label><label className="block"><div className="flex items-center justify-between"><span className="text-sm font-medium">{t('admin.password')}</span><button type="button" onClick={() => setShowPassword(!showPassword)} className="inline-flex items-center gap-1 font-mono text-[10px] uppercase tracking-[0.1em] text-muted-foreground">{showPassword ? <EyeOff size={13} /> : <Eye size={13} />} {showPassword ? 'Hide' : 'Show'}</button></div><input required type={showPassword ? 'text' : 'password'} value={password} onChange={event => { setPassword(event.target.value); setError(''); }} className="mt-2 w-full rounded-xl border border-border bg-card px-4 py-3.5 text-sm outline-none transition focus:border-primary" placeholder="admin123" /></label>{error && <div role="alert" className="rounded-lg border border-brand-terracotta/25 bg-terracotta px-4 py-3 text-sm text-terracotta-fg">{error}</div>}<div className="flex items-center justify-between"><button type="button" className="text-sm text-brand-indigo underline decoration-[#435a78]/30 underline-offset-4">{t('admin.forgot')}</button><PrimaryButton type="submit" icon={ArrowRight}>{t('admin.signIn')}</PrimaryButton></div></form><div className="mt-8 rounded-xl bg-saffron p-4 text-sm leading-6 text-saffron-fg"><strong>{t('admin.demoHint')}</strong><div className="mt-1 font-mono text-[11px]">admin@sahayak.demo · admin123</div></div><div className="mt-5 flex items-start gap-2 text-xs leading-5 text-muted-foreground"><ShieldCheck size={14} className="mt-0.5 shrink-0 text-primary" />{t('admin.security')}</div></div><div className="font-mono text-[10px] uppercase tracking-[0.14em] text-muted-foreground">SAHAYAK · Smart India Hackathon 2026 · {t('common.prototype')}</div></section></div></div>;
}
