import { useState } from 'react';
import { SectionLabel, Wordmark, Pill } from '../lib/ui';
import { ruleChecks } from '../lib/data';
import { ArrowRight, Check, CircleHelp, Database, Sparkles } from 'lucide-react';

export function RuleConsistencyPage() { const [review, setReview] = useState(false); return <><SectionLabel kicker="Verify · rule consistency" title="Rules should explain themselves." description="A clean inspection surface for deterministic eligibility logic, with assisted explanation layered on top." /><div className="grid gap-7 xl:grid-cols-[1fr_0.8fr]"><div className="border-y border-border"><div className="flex items-center justify-between py-4"><div><div className="font-display text-3xl">PM-KISAN</div><div className="mt-1 font-mono text-[10px] uppercase tracking-[0.14em] text-muted-foreground">Rule version 3.2 · updated 21 min ago</div></div><Pill tone={review ? 'saffron' : 'sage'}>{review ? 'Requires review' : 'Conditions verified'}</Pill></div><div className="space-y-0">{ruleChecks.map((check, i) => <div key={check.label} className="flex items-start gap-4 border-t border-border py-6"><div className={`mt-0.5 grid h-7 w-7 place-items-center rounded-full ${check.status === 'Verified' ? 'bg-sage text-primary' : 'bg-saffron text-saffron-fg'}`}>{check.status === 'Verified' ? <Check size={15} /> : <CircleHelp size={15} />}</div><div className="flex-1"><div className="text-sm font-medium">{check.label}</div><div className="mt-1 text-xs leading-5 text-muted-foreground">{check.detail}</div></div><div className="font-mono text-[9px] uppercase tracking-[0.12em] text-muted-foreground">{check.status}</div></div>)}</div><button onClick={() => setReview(!review)} className="mt-4 flex w-full items-center justify-between border-t border-border py-4 text-sm font-medium">{review ? 'Mark conditions verified' : 'Flag for rule review'} <ArrowRight size={15} /></button></div><div className="border-l border-border pl-0 xl:pl-7"><Wordmark tone="lavender">Assisted explanation</Wordmark><h2 className="mt-4 font-display text-4xl tracking-[-0.035em]">Readable by design.</h2><p className="mt-5 text-sm leading-6 text-muted-foreground">“PM-KISAN currently checks age, landholding and exclusion status. One exclusion category needs a human check before the rule can be treated as fully consistent.”</p><div className="mt-8 rounded-xl bg-lavender p-5 text-sm leading-6 text-lavender-fg"><div className="flex items-center gap-2 font-mono text-[9px] uppercase tracking-[0.14em]"><Sparkles size={13} /> Assisted explanation</div><div className="mt-3">AI assists with rule extraction and explanation. Deterministic rules remain auditable.</div></div><div className="mt-7 flex items-center gap-2 font-mono text-[10px] uppercase tracking-[0.12em] text-muted-foreground"><Database size={13} className="text-primary" /> Source: scheme registry · synthetic</div></div></div></> }
