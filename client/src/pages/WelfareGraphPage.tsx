import { useState } from 'react';
import { SectionLabel, Wordmark, Pill } from '../lib/ui';
import { GraphNode, GraphLink } from '../components/Graph';
import { Plus } from 'lucide-react';

export function WelfareGraphPage() {
  const [expanded, setExpanded] = useState(false);
  return <><SectionLabel kicker="Connect · welfare graph" title="Follow a signal through the system." description="Start focused. Expand only the relationship you need to explain." /><div className="grid gap-7 xl:grid-cols-[1.35fr_0.65fr]"><div className="relative min-h-[530px] overflow-hidden rounded-[1.5rem] border border-border bg-secondary p-6 md:p-10"><div className="absolute inset-0 opacity-40 [background-image:radial-gradient(color-mix(in_srgb,var(--foreground)_10%,transparent)_1px,transparent_1px)] [background-size:24px_24px]" /><div className="relative z-10 flex items-center justify-between"><Pill tone="indigo">Selected relationship</Pill><span className="font-mono text-[9px] uppercase tracking-[0.14em] text-muted-foreground">{expanded ? 'Expanded view' : 'Focused view'}</span></div><div className="relative z-10 mx-auto mt-16 max-w-2xl"><div className="flex flex-col items-center gap-0"><GraphNode label="NAJAFGARH" detail="Delhi · geography" tone="indigo" /><GraphLink text="142 potentially eligible households" /><GraphNode label="PM-KISAN" detail="Scheme · benefit" tone="saffron" /><GraphLink text="Eligibility rule · v3.2" /><GraphNode label="Potential benefit gap" detail="Requires human verification" tone="terracotta" active /></div>{expanded && <div className="mx-auto mt-6 grid max-w-lg grid-cols-2 gap-3"><div className="border-l border-primary pl-3"><div className="font-mono text-[9px] uppercase tracking-[0.12em] text-muted-foreground">Coverage</div><div className="mt-1 text-sm font-medium">78% recorded</div></div><div className="border-l border-brand-terracotta pl-3"><div className="font-mono text-[9px] uppercase tracking-[0.12em] text-muted-foreground">Signal</div><div className="mt-1 text-sm font-medium">1,284 households</div></div></div>}</div><button onClick={() => setExpanded(!expanded)} className="absolute bottom-6 left-1/2 flex -translate-x-1/2 items-center gap-2 rounded-full bg-foreground px-4 py-2.5 font-mono text-[10px] uppercase tracking-[0.14em] text-background">{expanded ? 'Collapse graph' : 'Expand relationship'} <Plus size={13} className={expanded ? 'rotate-45' : ''} /></button></div><div className="border-l border-border pl-0 xl:pl-7"><Wordmark tone="indigo">How to read it</Wordmark><h2 className="mt-4 font-display text-4xl tracking-[-0.035em]">One trace.<br />Several truths.</h2><p className="mt-5 text-sm leading-6 text-muted-foreground">The welfare graph turns a list of records into an explainable chain. Select one relationship, then expand when evidence needs to travel further.</p><div className="mt-8 space-y-4 border-t border-border pt-6">{[['Household cluster', '142 records match the current rule'], ['Scheme', 'PM-KISAN · farmer income support'], ['Geography', 'Najafgarh block · Delhi'], ['Action', 'Verify before outreach planning']].map(([label, value]) => <div key={label}><div className="font-mono text-[9px] uppercase tracking-[0.14em] text-muted-foreground">{label}</div><div className="mt-1 text-sm">{value}</div></div>)}</div></div></div></>;
}
